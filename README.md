# Elje_group

Run project
1. Clone this repository: run `git clone...` in your console
2. Run build project: run `npm start` ih your console
3. Project will open in your browser on http://localhost:3000